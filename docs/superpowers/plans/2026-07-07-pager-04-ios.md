# Pager 04 — iOS App（SwiftUI）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. **iOS 验证靠 xcodebuild + simctl，subagent 跑不了模拟器**——控制者在每个 task 后自己编译/运行/截图验证。

**Goal:** 交付 `ios/`：一个 SwiftUI iPhone app，连生产 hub（pager-hub.jianshuo.workers.dev），显示机器/会话列表，新建会话驱动本机 Claude Code，气泡流实时看工具调用、在 app 内和锁屏通知上批准权限。里程碑：**模拟器里的 app 连活的后端，新建会话让建硕的 Mac 上真 Claude Code 干活，事件流回 app，权限在 app 里点批准。**

**Architecture:** 瘦客户端（spec §4 + 02/03 落地约定 + 02-hub 终审给 04 的契约）。分层：`Protocol`（手写 Codable 镜像 @pager/protocol）→ `Keychain`（token 存储）→ `HubAPI`（REST）→ `ClientWS`（URLSession WebSocket）→ `AppModel`（会话/机器 store：按 (conv,seq) 去重、按 seq 排序、按 conv 过滤、patch 整段替换）→ SwiftUI Views → `PushManager`（APNs 注册 + 通知 action）。

**Tech Stack:** Swift 6 / SwiftUI，iOS 18，XcodeGen（project.yml），URLSession（REST + WebSocket）、UserNotifications。Debug 自动签名（模拟器免 profile），Release manual+match（留给 TestFlight skill）。

## 契约（02-hub 终审冻结的事件形状，客户端照此实现，不改协议包）

- `text` body `{markdown}`：流式经 `patch`（`{conv,eventId,markdown}`）**整段替换**同 eventId 的 markdown——**iOS 必须替换，不是追加**。
- `tool_card` body `{tool,title,summary,detail,diff?}`：可折叠卡片。
- `permission_request` body `{request_id,tool,description,options}`：带按钮；批准/拒绝走 `POST /api/permission-response`。status 保持 `running`，**「等待中」UI 由 permission_request 事件驱动，不是 status**（daemon 从不发 `waiting_input`）。
- `status` body `{state,note?}`，state ∈ thinking/running/done/failed。interrupt 目前表现为 `failed`（无独立 cancelled 态）。
- **单条 WS** 连 UserDO：`event`/`patch`/`machine_status` 全从这条下发。客户端按 **(conv,seq) 去重、按 seq 排序、按 conv 过滤**；backlog（subscribe 补历史）与直播可交错，**容忍乱序、去重**，只会不丢不重。
- 新建会话返回 **502 = 已创建但失败**（会话+首条+failed 状态已存在，会出现在列表）——iOS 视作「已创建但失败」，非「什么都没发生」。
- 发送消息以**广播回显**（自己发的事件被广播回来）为 ack；超时无回显 = 发送失败。
- `EventLoose.body` 可能缺失/未知 type → **未知 type 渲染通用卡片**（前向兼容，daemon 先加事件类型时旧 app 优雅降级）。

## 落地修正（执行后补记，代码为准）

- **协议 Codable**：`Event` 按顶层 type 分发 + `.unknown` 前向兼容；wire 字段名对 event.ts/wire.ts 逐字核对（request_id/afterSeq/eventId/machineId/deviceToken）。
- **依赖/工具**：Xcode 26.6 / Swift 6.3 / iOS 18 / XcodeGen 2.45.4；全程 `@MainActor`（AppModel/ClientWS/AppDelegate），Event/ServerMessage 不跨 actor，免 Sendable 体操。
- **APNs Swift6 坑**：UNUserNotificationCenterDelegate 方法必须 `nonisolated`，先从 response 取 Sendable 字符串再 `await MainActor.run` 碰 model。
- **★终审修的真 bug**：ClientWS `connect()` 每次 `resetBackoff()` → 指数退避永远回到 1s → 持续失败(如坏 token)变成 1s 重连风暴。改为 onConnected+resetBackoff 只在首帧成功时触发（didSignalConnect 守卫），重连才真退避。
- **安全**：Keychain 用 `...ThisDeviceOnly`（token 不进备份跨设备）；`PAGER_DEBUG_TOKEN`/`PAGER_DEBUG_OPEN_CONV` 加 `#if DEBUG` 门（不进 Release）。
- **活体验证**：模拟器 Debug build 用 `SIMCTL_CHILD_PAGER_DEBUG_TOKEN` 注入 token、`PAGER_DEBUG_OPEN_CONV` 直达会话（避 openurl 系统弹窗）；`pager://conversation/<id>` 深链复用于 APNs 点按。
- **TestFlight 前必做**（本计划 deferred，别忘）：`aps-environment` 从 `development` 改 `production`（分发走生产 APNs 网关）；配 fastlane `match`（`/wjs-publishing-testflight`）；hub 配 APNS_* secrets(.p8)。App Store 名 "Pager" 大概率被占，用「Pager 传呼机」类。

## Global Constraints

- `ios/` 目录，bundle id `com.wangjianshuo.Pager`，Team `97XBW2A43H`，iOS 18，Swift 6，`TARGETED_DEVICE_FAMILY "1"`（iPhone）。Debug 自动签名，Release manual（`match AppStore com.wangjianshuo.Pager`，本计划不配 match）。
- 生产 hub `https://pager-hub.jianshuo.workers.dev`；client token 在 `hub/.secrets.production.local`（`CLIENT_TOKEN`）。v1 配对从简：设置页粘贴 token（配对码流程留后续）。
- 协议 Codable 手写，快照测试对齐真 JSON（Swift 侧不引 zod）。
- 每 task 后控制者验证：`cd ios && xcodegen generate && xcodebuild -scheme Pager -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' build`；跑 UI 用 `simctl boot/install/launch` + 截图。
- 浅色 UI 优先（用户偏好）。
- conventional commits。

---

### Task 1: Xcode 工程 + 协议 Codable 模型 + 快照测试 + 模拟器编译启动

**Files:**
- Create: `ios/project.yml`（XcodeGen）
- Create: `ios/PagerApp/Info.plist`
- Create: `ios/PagerApp/Pager.entitlements`（aps-environment: development）
- Create: `ios/PagerApp/PagerApp.swift`（@main）
- Create: `ios/PagerApp/Protocol.swift`（Codable 模型）
- Create: `ios/PagerApp/ContentView.swift`（占位：显示 "Pager" + 协议自检结果）
- Create: `ios/PagerApp/Assets.xcassets/`（AppIcon + AccentColor 占位）
- Create: `ios/PagerTests/ProtocolTests.swift`
- Create: `ios/.gitignore`（忽略 `*.xcodeproj`、`DerivedData/`、`Secrets.xcconfig`）

**Interfaces:**
- Produces:
  - `Event`（struct：id/conv/seq/ts/role/agent + `body: EventBody`），`EventBody`（enum：`.text(markdown)` / `.toolCard(tool,title,summary,detail,diff?)` / `.permissionRequest(requestId,tool,description,options)` / `.permissionResponse(requestId,choice)` / `.status(state,note?)` / `.error(message,recoverable)` / `.unknown(type,raw)`——按顶层 `type` 分发，未知 type 落 `.unknown`，前向兼容）。
  - `MachineSummary`（id/name/online/dirs）、`ConversationSummary`（id/machineId/machineName/dir/state/lastMessage/lastSeq/updatedAt）。
  - 客户端上行 wire：`ClientMessage`（`.subscribe(conv,afterSeq)` / `.send(event: EventDraft)`）；下行 `ServerMessage`（`.event(Event)` / `.patch(conv,eventId,markdown)` / `.machineStatus(machine,online)`）。Encodable/Decodable 对齐协议包 wire.ts。
  - `EventDraft`（无 seq，用于上行；Encodable）。

- [ ] **Step 1: 写 project.yml**（镜像 VoiceDrop 约定，单 target，无扩展）

```yaml
name: Pager
options:
  bundleIdPrefix: com.wangjianshuo
  deploymentTarget:
    iOS: "18.0"
targets:
  Pager:
    type: application
    platform: iOS
    deploymentTarget: "18.0"
    sources:
      - path: PagerApp
    info:
      path: PagerApp/Info.plist
      properties:
        CFBundleDisplayName: Pager
        UILaunchScreen:
          UIColorName: ""
        UIBackgroundModes:
          - remote-notification
    entitlements:
      path: PagerApp/Pager.entitlements
      properties:
        aps-environment: development
    settings:
      base:
        SWIFT_VERSION: "6.0"
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        PRODUCT_BUNDLE_IDENTIFIER: com.wangjianshuo.Pager
        DEVELOPMENT_TEAM: 97XBW2A43H
        TARGETED_DEVICE_FAMILY: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      configs:
        Release:
          CODE_SIGN_STYLE: Manual
          CODE_SIGN_IDENTITY: "Apple Distribution"
          PROVISIONING_PROFILE_SPECIFIER: "match AppStore com.wangjianshuo.Pager"
  PagerTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "18.0"
    sources:
      - path: PagerTests
    dependencies:
      - target: Pager
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.wangjianshuo.Pager.Tests
        DEVELOPMENT_TEAM: 97XBW2A43H
        GENERATE_INFOPLIST_FILE: "YES"
schemes:
  Pager:
    build:
      targets:
        Pager: all
        PagerTests: [test]
    test:
      targets:
        - PagerTests
```

- [ ] **Step 2: Info.plist / entitlements / Assets 占位 / @main / ContentView 占位**

`Info.plist`：最小（XcodeGen 从 properties 合成大部分，此文件放空 dict 即可）：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0"><dict/></plist>
```

`Pager.entitlements`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0"><dict>
  <key>aps-environment</key><string>development</string>
</dict></plist>
```

Assets：建 `Assets.xcassets/Contents.json`（`{"info":{"author":"xcode","version":1}}`）、`AppIcon.appiconset/Contents.json`（空 iOS appicon set：`{"images":[],"info":{"author":"xcode","version":1}}`——占位，图标留后）、`AccentColor.colorset/Contents.json`。

`PagerApp.swift`：

```swift
import SwiftUI

@main
struct PagerApp: App {
    var body: some Scene {
        WindowGroup { ContentView() }
    }
}
```

`ContentView.swift`（占位 + 协议自检——解码一条内嵌 JSON，证明模型可用）：

```swift
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Pager").font(.largeTitle.bold())
            Text(selfCheck()).font(.footnote).foregroundStyle(.secondary)
        }
        .padding()
    }
    private func selfCheck() -> String {
        let json = #"{"id":"evt_1","conv":"cnv_1","seq":1,"ts":1751780000,"role":"agent","agent":"claude-code","type":"text","body":{"markdown":"hi"}}"#
        do {
            let e = try JSONDecoder().decode(Event.self, from: Data(json.utf8))
            if case .text(let md) = e.body { return "协议 OK：\(md)" }
            return "协议解码到非 text"
        } catch { return "协议解码失败：\(error)" }
    }
}
```

- [ ] **Step 3: 写 Protocol.swift（Codable 模型）**

按 `@pager/protocol` 的 event.ts / wire.ts / api.ts 手写。核心：`EventBody` 按顶层 `type` 手写 `init(from:)`/`encode(to:)`，未知 type 落 `.unknown` 保 raw。给出完整实现：

```swift
import Foundation

enum EventBody {
    case text(markdown: String)
    case toolCard(tool: String, title: String, summary: String, detail: String, diff: String?)
    case permissionRequest(requestId: String, tool: String, description: String, options: [String])
    case permissionResponse(requestId: String, choice: String)
    case status(state: String, note: String?)
    case error(message: String, recoverable: Bool)
    case unknown(type: String, raw: [String: JSONValue])
}

struct Event: Decodable, Identifiable {
    let id: String
    let conv: String
    let seq: Int
    let ts: Int
    let role: String
    let agent: String
    let type: String
    let body: EventBody

    enum CodingKeys: String, CodingKey { case id, conv, seq, ts, role, agent, type, body }

    init(from d: Decoder) throws {
        let c = try d.container(keyedBy: CodingKeys.self)
        id = try c.decode(String.self, forKey: .id)
        conv = try c.decode(String.self, forKey: .conv)
        seq = try c.decode(Int.self, forKey: .seq)
        ts = try c.decode(Int.self, forKey: .ts)
        role = try c.decode(String.self, forKey: .role)
        agent = (try? c.decode(String.self, forKey: .agent)) ?? "claude-code"
        type = try c.decode(String.self, forKey: .type)
        let b = try c.nestedContainer(keyedBy: BodyKeys.self, forKey: .body)
        body = try Event.decodeBody(type: type, b, raw: c, forKey: .body)
    }

    enum BodyKeys: String, CodingKey {
        case markdown, tool, title, summary, detail, diff
        case request_id, description, options, choice, state, note, message, recoverable
    }

    static func decodeBody(type: String, _ b: KeyedDecodingContainer<BodyKeys>,
                           raw: KeyedDecodingContainer<CodingKeys>, forKey: CodingKeys) throws -> EventBody {
        switch type {
        case "text":
            return .text(markdown: (try? b.decode(String.self, forKey: .markdown)) ?? "")
        case "tool_card":
            return .toolCard(
                tool: (try? b.decode(String.self, forKey: .tool)) ?? "",
                title: (try? b.decode(String.self, forKey: .title)) ?? "",
                summary: (try? b.decode(String.self, forKey: .summary)) ?? "",
                detail: (try? b.decode(String.self, forKey: .detail)) ?? "",
                diff: try? b.decode(String.self, forKey: .diff))
        case "permission_request":
            return .permissionRequest(
                requestId: (try? b.decode(String.self, forKey: .request_id)) ?? "",
                tool: (try? b.decode(String.self, forKey: .tool)) ?? "",
                description: (try? b.decode(String.self, forKey: .description)) ?? "",
                options: (try? b.decode([String].self, forKey: .options)) ?? [])
        case "permission_response":
            return .permissionResponse(
                requestId: (try? b.decode(String.self, forKey: .request_id)) ?? "",
                choice: (try? b.decode(String.self, forKey: .choice)) ?? "")
        case "status":
            return .status(state: (try? b.decode(String.self, forKey: .state)) ?? "",
                           note: try? b.decode(String.self, forKey: .note))
        case "error":
            return .error(message: (try? b.decode(String.self, forKey: .message)) ?? "",
                          recoverable: (try? b.decode(Bool.self, forKey: .recoverable)) ?? false)
        default:
            let rawBody = (try? raw.decode([String: JSONValue].self, forKey: forKey)) ?? [:]
            return .unknown(type: type, raw: rawBody)
        }
    }
}

// 最小 JSON 值类型，装未知 body
enum JSONValue: Codable {
    case string(String), number(Double), bool(Bool), object([String: JSONValue]), array([JSONValue]), null
    init(from d: Decoder) throws {
        let c = try d.singleValueContainer()
        if c.decodeNil() { self = .null }
        else if let b = try? c.decode(Bool.self) { self = .bool(b) }
        else if let n = try? c.decode(Double.self) { self = .number(n) }
        else if let s = try? c.decode(String.self) { self = .string(s) }
        else if let a = try? c.decode([JSONValue].self) { self = .array(a) }
        else if let o = try? c.decode([String: JSONValue].self) { self = .object(o) }
        else { self = .null }
    }
    func encode(to e: Encoder) throws {
        var c = e.singleValueContainer()
        switch self {
        case .string(let s): try c.encode(s)
        case .number(let n): try c.encode(n)
        case .bool(let b): try c.encode(b)
        case .object(let o): try c.encode(o)
        case .array(let a): try c.encode(a)
        case .null: try c.encodeNil()
        }
    }
}

struct MachineSummary: Decodable, Identifiable {
    let id: String; let name: String; let online: Bool; let dirs: [String]
}
struct ConversationSummary: Decodable, Identifiable {
    let id: String; let machineId: String; let machineName: String; let dir: String
    let state: String; let lastMessage: String; let lastSeq: Int; let updatedAt: Int
}

// 上行草稿（无 seq）
struct EventDraft: Encodable {
    let id: String; let conv: String; let ts: Int; let role: String; let agent: String
    let type: String; let body: [String: JSONValue]
}

// 客户端 → hub
enum ClientMessage: Encodable {
    case subscribe(conv: String, afterSeq: Int)
    case send(conv: String, event: EventDraft)
    func encode(to e: Encoder) throws {
        var c = e.container(keyedBy: K.self)
        switch self {
        case .subscribe(let conv, let after):
            try c.encode("subscribe", forKey: .kind); try c.encode(conv, forKey: .conv); try c.encode(after, forKey: .afterSeq)
        case .send(let conv, let event):
            try c.encode("send", forKey: .kind); try c.encode(conv, forKey: .conv); try c.encode(event, forKey: .event)
        }
    }
    enum K: String, CodingKey { case kind, conv, afterSeq, event }
}

// hub → 客户端
enum ServerMessage: Decodable {
    case event(Event)
    case patch(conv: String, eventId: String, markdown: String)
    case machineStatus(machine: MachineSummaryLite, online: Bool)
    struct MachineSummaryLite: Decodable { let id: String; let name: String }
    enum K: String, CodingKey { case kind, event, conv, eventId, markdown, machine, online }
    init(from d: Decoder) throws {
        let c = try d.container(keyedBy: K.self)
        switch try c.decode(String.self, forKey: .kind) {
        case "event": self = .event(try c.decode(Event.self, forKey: .event))
        case "patch": self = .patch(conv: try c.decode(String.self, forKey: .conv),
                                    eventId: try c.decode(String.self, forKey: .eventId),
                                    markdown: try c.decode(String.self, forKey: .markdown))
        case "machine_status": self = .machineStatus(machine: try c.decode(MachineSummaryLite.self, forKey: .machine),
                                                     online: try c.decode(Bool.self, forKey: .online))
        case let k: throw DecodingError.dataCorruptedError(forKey: .kind, in: c, debugDescription: "unknown kind \(k)")
        }
    }
}
```

- [ ] **Step 4: 写快照测试**

`ProtocolTests.swift`：用真实 JSON（对齐协议包 fixture 形状）断言解码：text 事件、tool_card、permission_request、status、未知 type→.unknown、ServerMessage 的 event/patch/machine_status、ClientMessage 编码含正确 kind。（写 8-10 个 XCTest 断言。）

- [ ] **Step 5: 生成工程 + 编译 + 跑测试 + 模拟器启动截图**

```bash
cd ios && xcodegen generate
xcodebuild -scheme Pager -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' build 2>&1 | tail -5
xcodebuild -scheme Pager -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' test 2>&1 | tail -15
# 启动看一眼
xcrun simctl boot "iPhone 17" 2>/dev/null || true
xcodebuild -scheme Pager -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath /tmp/pager-dd build
xcrun simctl install "iPhone 17" "$(find /tmp/pager-dd -name Pager.app -path '*iphonesimulator*' | head -1)"
xcrun simctl launch "iPhone 17" com.wangjianshuo.Pager
xcrun simctl io "iPhone 17" screenshot /tmp/pager-launch.png
```

Expected: build 成功、测试全绿、截图显示 "Pager" + "协议 OK：hi"。控制者看截图确认。

- [ ] **Step 6: Commit**

```bash
git add ios && git commit -m "feat(ios): XcodeGen 工程、协议 Codable 模型与快照测试（模拟器编译启动通过）"
```

---

### Task 2: Keychain token 存储 + HubAPI（REST）+ 设置页 + 活体连通

**Files:**
- Create: `ios/PagerApp/Keychain.swift`（存 client token + hubUrl）
- Create: `ios/PagerApp/HubAPI.swift`
- Create: `ios/PagerApp/SettingsView.swift`
- Modify: `ios/PagerApp/ContentView.swift`（加设置入口 + 拉机器列表自检）
- Test: `ios/PagerTests/HubAPITests.swift`

**Interfaces:**
- `Keychain`：`static token: String?` / `hubUrl: String`（默认生产 URL）get/set，走 `SecItem`。
- `HubAPI(actor)`：`machines() async throws -> [MachineSummary]`、`conversations() async throws -> [ConversationSummary]`、`newConversation(machineId,dir,message) async throws -> NewConvResult`（`.created(convId)` / `.createdButFailed(convId)`（502）/ 其它抛）、`permissionResponse(conv,requestId,choice)`、`registerDevice(token)`。所有请求带 `Authorization: Bearer <token>`。
- `SettingsView`：粘贴/编辑 client token + hub url，存 Keychain。

- [ ] **Step 1: Keychain + HubAPI + SettingsView + ContentView 自检（拉机器列表显示在线数）**

（实现从简：URLSession + async/await；502 特判返回 createdButFailed；JSON body 用 Task 1 的模型。）

- [ ] **Step 2: 单测（mock URLProtocol）**：机器列表解码、新建会话 201→created / 502→createdButFailed / 409→抛「机器离线」/ 400→抛「目录不允许」。

- [ ] **Step 3: 编译 + 测试 + 活体连通**：把 CLIENT_TOKEN 填进模拟器 app（设置页手填，或起动时读环境注入的 debug 默认值），拉 `/api/machines`，截图显示 `建硕的 Mac 在线`。

```bash
# 活体：设置页填 token 后，ContentView 显示在线机器；截图确认 mch_mac online
```

- [ ] **Step 4: Commit** `feat(ios): Keychain token 存储、HubAPI REST 客户端与设置页`

---

### Task 3: ClientWS（WebSocket）+ AppModel（去重/排序/过滤/patch）+ 流式

**Files:**
- Create: `ios/PagerApp/ClientWS.swift`（URLSessionWebSocketTask）
- Create: `ios/PagerApp/AppModel.swift`（`@MainActor @Observable`）
- Test: `ios/PagerTests/AppModelTests.swift`

**Interfaces:**
- `ClientWS`：`connect()`（`/ws/client`，Bearer via URLRequest header）、`subscribe(conv,afterSeq)`、`send(ClientMessage)`、`onMessage(ServerMessage)->Void`；断线重连（指数退避）；前台连、后台断。
- `AppModel`：持有 `machines: [String: MachineSummary]`、`conversations: [ConversationSummary]`（首页，走 REST 刷新 + machine_status/notify 增量）、`events: [String: [Event]]`（按 conv）。`ingest(ServerMessage)`：event→按 (conv,seq) 去重插入并按 seq 排序；patch→找 conv 内该 eventId 的 text 事件替换 markdown；machine_status→更新 machines。`openConversation(conv)`：WS subscribe(afterSeq=本地已有最大 seq)。`sendText(conv,markdown)`：REST or WS send，乐观插入，等回显 ack。

- [ ] **Step 1: 单测**：ingest 去重（同 seq 不重复）、乱序（seq 3 先到再 seq 2，最终有序）、patch 替换 markdown、machine_status 更新在线态、未知 type 事件也入列（通用卡片）。
- [ ] **Step 2-3: 实现 + 编译 + 测试 + 活体**：连 WS，subscribe 一个已有会话（用 Task 2 建的），收到 backlog 事件，截图/日志确认。
- [ ] **Step 4: Commit** `feat(ios): WebSocket 客户端与 AppModel 事件 store（去重/排序/patch）`

---

### Task 4: SwiftUI UI——会话列表 + 会话页 + 新建会话（活体驱动 Mac）

**Files:**
- Create: `ios/PagerApp/ConversationListView.swift`（首页）
- Create: `ios/PagerApp/ConversationView.swift`（气泡流）
- Create: `ios/PagerApp/NewConversationView.swift`
- Create: `ios/PagerApp/EventRowViews.swift`（text/tool_card/permission_request/status/通用卡片渲染）
- Modify: `ios/PagerApp/ContentView.swift`（改成 NavigationStack 首页）、`PagerApp.swift`（注入 AppModel）

**Interfaces / 行为:**
- 首页：顶部机器在线横条；会话行（机器名+目录、最后摘要、状态点 🟢running/🟠permission待批/⚪️done/🔴failed、未读数）；下拉刷新走 REST。
- 会话页：气泡流——用户消息右对齐；`text` markdown（流式，patch 替换）；`tool_card` 可折叠（标题+展开 detail/diff）；`permission_request` 带「允许/拒绝」按钮，点了走 REST，按钮变已决态；`status` 细行；**未知 type → 通用卡片**（显示 type + 摘要）。底部输入框发消息（麦克风按钮位留 v2）。
- 新建会话：选机器（在线亮/离线灰）→ 选目录（machine.dirs）→ 输首条 → POST；502 也进会话（标失败）。
- 浅色 UI。

- [ ] **Step 1: 实现所有视图 + 接 AppModel。**
- [ ] **Step 2: 编译 + 模拟器 UI 驱动活体后端（关键验收）**：
  - 起 app（token 已配）→ 首页显示「建硕的 Mac 在线」
  - 新建会话 → 选 mch_mac → 选 /tmp/pager-e2e → 发「创建 hello.txt 写 hi」
  - 会话页看到 running → tool_card（pwd）→ permission_request（Write）
  - **在 app 里点「允许」** → tool_card（Write）→ done
  - 截图每一步；确认 `/tmp/pager-e2e/hello.txt` 真被建出来
  （模拟器 UI 自动化：用 `xcrun simctl` + 若有 `xcuitest` 或手动点击后截图；控制者可用 accessibility 逐步驱动或至少截图人工确认关键屏。）
- [ ] **Step 3: Commit** `feat(ios): 会话列表/会话页/新建会话 UI——模拟器驱动真机 Claude Code 通过`

---

### Task 5: APNs——设备注册 + 通知 action + 锁屏批准（模拟推送验证）

**Files:**
- Create: `ios/PagerApp/PushManager.swift`（AppDelegate + UNUserNotificationCenterDelegate）
- Modify: `ios/PagerApp/PagerApp.swift`（`@UIApplicationDelegateAdaptor`）
- Create: `ios/scripts/sim-push.sh`（`simctl push` 模拟一条 permission_request 推送）

**Interfaces / 行为:**
- 启动请求通知权限 → 拿 APNs device token → `HubAPI.registerDevice`。
- 注册 `UNNotificationCategory("PERMISSION_REQUEST")` 带 `allow`/`deny` 两个 action。
- 收到点击 action → 从 payload 取 `conv`/`request_id` → `POST /api/permission-response`（**不开 app 也能批准**）。
- 前台收到推送 → 也能 in-app 展示。
- **验证（模拟器 simctl push，无需真 Apple 推送链路）**：`sim-push.sh` 推一条 `{aps:{alert,category:"PERMISSION_REQUEST"},conv,request_id}` → 通知出现 → 触发 allow action → 断言 `/api/permission-response` 打到 hub（可配合一个真 pending 权限的会话，看 daemon 是否放行）。
- 真 APNs 投递（hub 发推）需 .p8 + bundle 注册 = TestFlight 期事项（本 task 只建客户端 + 本地模拟推送验证；hub 的 APNS_* secrets 那时再配）。

- [ ] **Step 1-2: 实现 + 编译 + simctl push 验证 + 截图。**
- [ ] **Step 3: Commit** `feat(ios): APNs 设备注册、权限通知 action 与锁屏批准（模拟推送验证）`

---

### Task 6: 整分支终审 + 合入 main push

- 整分支 review（fable/opus）：Swift 并发正确性（@MainActor、actor 边界、Sendable）、Codable 与协议契约一致、WS 重连/去重、内存/循环引用、APNs 逻辑、UI 状态。
- 修 Critical/Important，ff 合入 main，push。
- 记录：真 TestFlight 上架走 `/wjs-publishing-testflight` 再 `/wjs-publishing-appstore`（App Store 名 "Pager" 大概率被占，用「Pager 传呼机」类）。

## 明确不做（v1）

- 配对码换 token（先手填 token）。
- 语音输入/播报（麦克风按钮位留着）。
- 真 APNs 投递链路（本计划到模拟推送为止，hub secrets + 上架留 TestFlight 期）。
- iPad / 深色专门适配（浅色优先）。
